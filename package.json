{
	"name": "redwatch",
	"version": "1.0.0",
	"description": "Reddit sentiment analysis and monitoring tool",
	"private": true,
	"scripts": {
		"install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
		"dev": "cd frontend && npm run dev",
		"dev:backend": "cd backend && npm run dev"
	}
}